# Variables Exercise

Complete the TODOs in `main.js`.

## Requirements
- Define variables:
  - `firstName` (string)
  - `lastName` (string)
  - `age` (number, declared with `let` because you'll change it)
  - `fullName` (string: `${firstName} ${lastName}`)
- Print a greeting to the console using these variables.
- Change the `age` variable to a different value (to show `let` works).

Export the variables at the bottom so tests can read them:
```js
module.exports = { firstName, lastName, age, fullName };
